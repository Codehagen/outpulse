# -----------------------------------------------
# Database Connection
# -----------------------------------------------
# Connection string for the database (with connection pooling)
DATABASE_URL="postgresql://username:password@localhost:5432/mydatabase?pgbouncer=true"

# Direct connection to the database (for Prisma migrations)
DIRECT_URL="postgresql://username:password@localhost:5432/mydatabase"

# -----------------------------------------------
# Authentication (Better Auth)
# -----------------------------------------------
# A random secret for encryption and hashing
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your_random_secret_key_here

# Base URL of your application
BETTER_AUTH_URL=http://localhost:3000

# -----------------------------------------------
# OAuth Providers
# -----------------------------------------------
# Google OAuth credentials
# Get from Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# -----------------------------------------------
# Application Settings
# -----------------------------------------------
# Public URL of your application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------
# Optional: Email Service
# -----------------------------------------------
# SMTP settings for sending emails (verification, password reset)
# EMAIL_SERVER=smtp://username:password@smtp.example.com:587
# EMAIL_FROM=noreply@example.com 